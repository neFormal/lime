server:
  ip: 0.0.0.0
  port: 4400

shards:
  user_1:
    host: localhost
    user: user
    password: password
    database: lime
    min_pool_size: 2
    max_pool_size: 5
    connection_idle_lifetime: 30

game_configs_path: "../../config/Config/"
